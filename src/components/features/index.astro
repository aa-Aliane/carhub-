---
import styles from './Features.module.css';
import phoneMockup from '../../assets/5.jpeg';
import { Image } from 'astro:assets';
---

<section class={styles.features} id="features">
  <div class={styles.container}>
    <div class={styles.header}>
      <h2 class={styles.title}>
        Comment CareHub <span class={styles.accent}>change la donne</span>
      </h2>
    </div>

    <div class={styles.mainContent}>
      <div class={styles.column}>
        <div class={styles.featureItem}>
          <div class={styles.number}>01</div>
          <h3>Diagnostic IA</h3>
          <p>
            Saisissez vos symptômes et laissez notre intelligence prédictive
            vous orienter instantanément.
          </p>
        </div>
        <div class={styles.featureItem}>
          <div class={styles.number}>02</div>
          <h3>Téléconsultation</h3>
          <p>
            Un échange direct avec un expert certifié en moins de 15 minutes,
            sans déplacement.
          </p>
        </div>
      </div>

      <div class={styles.phoneCenter}>
        {/* Optimisation automatique en WebP via Astro Assets */}
        <Image
          src={phoneMockup}
          alt="Interface de l'application CareHub"
          format="webp"
          width={300}
          height={600}
          quality="high"
          class={styles.phoneImg}
        />
        <div class={styles.circleDecor}></div>
      </div>

      <div class={styles.column}>
        <div class={styles.featureItem}>
          <div class={styles.number}>03</div>
          <h3>Pharmacie partenaire</h3>
          <p>Transfert automatique vers la pharmacie la plus proche.</p>
        </div>
        <div class={styles.featureItem}>
          <div class={styles.number}>04</div>
          <h3>Livraison Express</h3>
          <p>
            Vos médicaments livrés à domicile depuis la pharmacie la plus proche
            de chez vous.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
